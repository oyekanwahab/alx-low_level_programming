#include <stdio.h>

/**
 * main - Print using for loop.
 * Description:	'This retirate all the alphabet'
 *
 * Return: 0 if sucess
 *
 */

int main(void)
{
	char ch;

	for (ch = 'A'; ch <= 'Z'; ch++)
		putchar('%c', ch)
	puthcar("\n");
	return (0);
}
