#include <stdio.h>

/**
 * main - Print using for loop.
 * Description:	'This retirate all the alphabet'
 *
 * Return: 0 if sucess
 *
 */

int main(void)
{
	char num;

	for (num = 0; ch <= 9; ch++)
		putchar(ch);
	putchar('\n');
	return (0);
}
