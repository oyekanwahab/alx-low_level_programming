#include <stdio.h>

/**
 * main - Print using for loop.
 * Description:	'This retirate all the alphabet'
 *
 * Return: 0 if sucess
 *
 */

int main(void)
{
	int i;

	for (int i = 0; i <= 9; i++)
		putchar("%d", i);
	putchar("\n")
	return (0);
}
