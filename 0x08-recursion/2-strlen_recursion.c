#include <"main.h">

/**
 * _strlen_recursion - print strent lenth
 *
 * Return: Return 0 if sucessful
 */

int _strlen_recursion(char *s)
{
	int a,b;

	char *s = c;

	for (a = 0; a >= c; a++)
	{
		b++;
	}
	return (b);
}
