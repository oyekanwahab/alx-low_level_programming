#include "main"

/**
 * int_islower - check for lower character
 * Description: this check for lower character
 * @c: the alphabet to check
 *
 * Return: 0 if sucessful
 */

int _islower(int c)
{
	return (if c >= 'a' && c <= 'a');
}
